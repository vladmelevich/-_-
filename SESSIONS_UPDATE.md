# Обновление системы сессий

## ✅ Что было выполнено:

### 1. **Хранение сессий на сервере (sessions.json)**
- ✅ Создан файл `server/data/sessions.json` для хранения активных сессий
- ✅ Сессии теперь хранятся централизованно на сервере, а не в localStorage браузера
- ✅ Автоматическая очистка при запуске сервера

### 2. **API для работы с сессиями**
Добавлены следующие эндпоинты в `server/server.js`:

```javascript
GET    /api/sessions              // Получить все активные сессии
POST   /api/sessions              // Создать новую сессию
DELETE /api/sessions/:sessionId   // Удалить конкретную сессию
DELETE /api/sessions              // Очистить все сессии
```

### 3. **Очистка сессий при запуске**
- ✅ При запуске сервера файл `sessions.json` автоматически очищается
- ✅ Гарантирует, что при перезапуске не будет старых "мертвых" сессий
- ✅ Лог в консоли: `🧹 Sessions cleared on server startup`

### 4. **Frontend обновлен**
#### `main/src/utils/api.js`
Добавлены методы:
- `api.getSessions()` - получить все сессии
- `api.createSession(sessionData)` - создать сессию
- `api.deleteSession(sessionId)` - удалить сессию
- `api.clearAllSessions()` - очистить все

#### `main/src/pages/SessionsPage.jsx`
- ✅ Переработан для загрузки сессий с сервера
- ✅ Автообновление каждые 3 секунды
- ✅ Удалена зависимость от localStorage

#### `main/src/components/ModePage.jsx`
- ✅ Создание сессий теперь через API
- ✅ Загрузка сессий с сервера
- ✅ Удалена синхронизация через localStorage
- ✅ Автообновление списка сессий каждые 3 секунды

### 5. **Валидация и фильтрация**
- ✅ Сервер автоматически отфильтровывает сессии старше 1 часа
- ✅ Возвращает только валидные активные сессии
- ✅ При обнаружении невалидных сессий автоматически очищает их из файла

## 📁 Измененные файлы:

### Сервер:
1. `server/server.js` - добавлен API для сессий
2. `server/data/sessions.json` - создан (очищается при запуске)

### Frontend:
1. `main/src/utils/api.js` - добавлены методы для работы с API сессий
2. `main/src/pages/SessionsPage.jsx` - обновлен для работы с API
3. `main/src/components/ModePage.jsx` - обновлен для работы с API

## 🚀 Как это работает:

### Создание сессии:
1. Пользователь нажимает "Создать сессию"
2. Frontend отправляет POST запрос на `/api/sessions`
3. Сервер создает сессию с уникальным ID
4. Сессия сохраняется в `sessions.json`
5. Сессия возвращается клиенту
6. Все открытые вкладки получают обновление

### Просмотр сессий:
1. Компоненты отправляют GET запрос на `/api/sessions`
2. Сервер фильтрует валидные сессии (< 1 часа)
3. Возвращает список активных сессий
4. Автообновление каждые 3 секунды

### Очистка при запуске:
1. При запуске `npm run server`
2. Функция `initDataDir()` вызывается первой
3. Файл `sessions.json` перезаписывается пустым массивом `[]`
4. Сообщение в консоли: `🧹 Sessions cleared on server startup`
5. Гарантия чистого старта без старых сессий

## 🔧 Преимущества новой системы:

1. **Централизованное хранение** - все сессии в одном месте
2. **Синхронизация между вкладками** - автоматическая через API
3. **Очистка при запуске** - нет старых "зависших" сессий
4. **Валидация на сервере** - только актуальные сессии
5. **Масштабируемость** - легко добавить новые функции
6. **Безопасность** - сессии защищены JWT токеном

## 📝 Примечания:

- Сессии живут максимум 1 час
- При перезапуске сервера все сессии очищаются
- Для создания сессии требуется авторизация
- Просмотр сессий доступен всем

## ✅ Статус проекта:

- ✅ Страница вывода зачетов создана
- ✅ Уведомления при пополнении/выводе работают
- ✅ Сессии хранятся на сервере в sessions.json
- ✅ Автоочистка сессий при запуске
- ✅ Логика всех игр исправлена
- ✅ Нет ошибок линтера

Проект готов к тестированию! 🎉
