# MCB Casino - Полнофункциональное приложение

Полнофункциональное веб-приложение с React фронтендом и Node.js бэкендом.

## 🏗️ Архитектура

- **Фронтенд**: React + Vite + TailwindCSS
- **Бэкенд**: Node.js + Express
- **Аутентификация**: JWT токены
- **Хранение данных**: JSON файлы (можно легко перейти на БД)

## 📁 Структура проекта

```
tpo/
├── main/          # Фронтенд приложение
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   ├── context/        # React Context (Auth, Language, Notifications)
│   │   ├── pages/         # Страницы приложения
│   │   ├── utils/          # Утилиты (API клиент)
│   │   └── App.jsx         # Главный компонент
│   ├── package.json
│   └── vite.config.js
│
└── server/        # Бэкенд API
    ├── server.js           # Express сервер
    ├── data/               # Данные (users.json)
    ├── package.json
    └── README.md           # Документация API
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

**Вариант 1 - Установка всех зависимостей одной командой:**
```bash
npm run install:all
```

**Вариант 2 - Установка по отдельности:**

**Фронтенд:**
```bash
cd main
npm install
```

**Бэкенд:**
```bash
cd server
npm install
```

### 2. Настройка переменных окружения

**Бэкенд** - создайте `server/.env`:
```env
PORT=3001
JWT_SECRET=mcb-secret-key-change-in-production-2024
NODE_ENV=development
```

**Фронтенд** (опционально) - создайте `main/.env`:
```env
VITE_API_URL=http://localhost:3001/api
```

### 3. Запуск

**🚀 Запуск одной командой (рекомендуется):**
```bash
npm run dev
```

Эта команда запустит одновременно:
- **Бэкенд** на `http://localhost:3001`
- **Фронтенд** на `http://localhost:5173`

**Альтернативный способ - запуск в отдельных терминалах:**

**Терминал 1 - Бэкенд:**
```bash
cd server
npm run dev
```

**Терминал 2 - Фронтенд:**
```bash
cd main
npm run dev
```

## 🔐 Предустановленный администратор

- **Username:** `admin`
- **Password:** `admin`

## 📡 API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получение текущего пользователя
- `PUT /api/auth/user` - Обновление данных пользователя

### Пользователи (только для админа)
- `GET /api/users` - Все пользователи
- `GET /api/users/students` - Только студенты
- `PUT /api/users/:userId/credits` - Обновление зачетов

### Health Check
- `GET /api/health` - Проверка работоспособности

Подробная документация API: [server/README.md](server/README.md)

## ✨ Основные возможности

### Для пользователей:
- ✅ Регистрация и авторизация
- ✅ Игра в различные режимы (Dice, Coinflip, Blackjack, и др.)
- ✅ Система зачетов (начисление/списание)
- ✅ Статистика побед/поражений
- ✅ Профиль пользователя

### Для администраторов:
- ✅ Панель управления студентами
- ✅ Управление зачетами студентов
- ✅ Просмотр статистики всех пользователей
- ✅ Поиск по студентам

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации (срок действия: 24 часа)
- Защита маршрутов (требуется авторизация)
- Валидация входных данных на бэкенде
- CORS настроен для работы с фронтендом

## 📝 Особенности реализации

### Бэкенд полностью управляет:
- ✅ Хранением пользователей и паролей
- ✅ Аутентификацией и авторизацией
- ✅ Обновлением зачетов и статистики
- ✅ Валидацией всех операций
- ✅ Управлением данными студентов (для админов)

### Фронтенд:
- Отображает UI
- Отправляет запросы к API
- Управляет состоянием интерфейса
- Обрабатывает пользовательский ввод

## 🛠️ Технологии

**Фронтенд:**
- React 18
- React Router
- TailwindCSS
- Lucide React (иконки)
- Vite

**Бэкенд:**
- Node.js
- Express
- bcryptjs (хеширование паролей)
- jsonwebtoken (JWT)
- dotenv (переменные окружения)

## 📦 Развертывание

### Production сборка:

**Фронтенд:**
```bash
cd main
npm run build
```

**Бэкенд:**
```bash
cd server
npm start
```

Не забудьте:
- Изменить `JWT_SECRET` на безопасный ключ
- Настроить CORS для production домена
- Настроить переменную `FRONTEND_URL` в `.env`

## 🐛 Устранение неполадок

### Порт занят
Измените `PORT` в `server/.env`

### CORS ошибки
Убедитесь, что фронтенд запущен на правильном порту или обновите CORS настройки в `server/server.js`

### Ошибки подключения к API
1. Проверьте, что бэкенд запущен
2. Проверьте URL API в `main/src/utils/api.js`
3. Проверьте файрвол

## 📝 Последние обновления

### ✅ Система сессий (v2.1.0)
- **Автоматическое определение имени**: Имя игрока теперь берётся автоматически из `user.username`, убран выбор из списка
- **Отображение роли**: При создании сессии показывается "Ученик [Имя] создал сессию и ждёт присоединения" или "Преподаватель [Имя]..."
- **Упрощённый UX**: Меньше полей ввода, быстрее создание сессии

### ✅ Система уведомлений (v2.0.0)
- **Двойная система уведомлений**: 
  - NotificationsDropdown в навигации (сохраняет историю)
  - Toast всплывающие уведомления (автоматическое появление на 5 секунд)
- **Интеграция**: Уведомления при пополнении, выводе зачетов и игровых результатах
- **4 типа уведомлений**: success, warning, error, info

### ✅ Хранение сессий на сервере
- **Серверное хранилище**: `sessions.json` на сервере вместо localStorage
- **Автоочистка**: Сессии очищаются при запуске сервера
- **API endpoints**: GET, POST, DELETE для управления сессиями

### 📚 Новая документация
- `SESSION_UPDATES.md` - Подробное описание изменений системы сессий
- `NOTIFICATIONS.md` - Полная документация по системе уведомлений
- `PROJECT_STATUS.md` - Текущий статус проекта со всеми функциями

## 📄 Лицензия

Проект для учебных целей.
